## Directory for cluster definitions

Each YAML file corresponds to cluster and the name of the file should be
`CLUSTERNAME.yml`, where `CLUSTERNAME` is replaced with the exact name of the cluster on ECS.
